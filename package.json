{
  "name": "magic-progress",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test --test-isolation=none"
  },
  "dependencies": { "firebase": "^12.1.0" },
  "devDependencies": { "vite": "^7.1.1" }
}
